import Constants from 'expo-constants';

export default {
  apiUrl: Constants.manifest.extra.apiUrl,
  splashImage: Constants.manifest.splash.image,
  splashBackground: Constants.manifest.splash.backgroundColor,
};
